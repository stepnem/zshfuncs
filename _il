#compdef il
if (( CURRENT > 2 )); then
	local user=${words[2]}
	_files -W ~/.znc/users/$user/moddata/log
else
	#local expl
	#_description znc_users expl 'ZNC user names'
	#compadd "$expl[@]" 
	_files -W ~/.znc/users -/
fi
